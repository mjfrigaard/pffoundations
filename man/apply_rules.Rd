% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_rules.R
\name{apply_rules}
\alias{apply_rules}
\title{Apply Keyword Rules to Categorize Transactions}
\usage{
apply_rules(df, rules)
}
\arguments{
\item{df}{Data frame with a \code{Description} column (character)}

\item{rules}{Data frame with \code{Pattern} (regex, character) and \code{Category}
(character) columns}
}
\value{
Data frame with \code{Category} column added. The \code{Category} column
contains the matched rule category, or \code{"Other"} if no rule matched.
}
\description{
Apply keyword rules in order to automatically categorize transactions.
Rules are matched sequentially against the Description column; the first
matching pattern wins. If no rule matches, the transaction receives the
\code{"Other"} category.
}
\details{
This function implements the core logic for the Tracking Spending module's
categorization engine, allowing users to develop and refine rules for
organizing their transactions.
}
\examples{
# Sample transactions
trans <- data.frame(
  Description = c("Netflix charge", "Grocery store", "Unknown vendor"),
  Amount = c(-15, -50, -25),
  stringsAsFactors = FALSE
)

# Sample rules
rules <- data.frame(
  Pattern = c("Netflix|Hulu|Spotify", "Safeway|Whole Foods"),
  Category = c("Subscriptions", "Groceries"),
  stringsAsFactors = FALSE
)

# Apply rules
apply_rules(trans, rules)

}
\seealso{
Other utils:
\code{\link[=fmt_dollar]{fmt_dollar()}},
\code{\link[=fmt_pct]{fmt_pct()}}

Other tracking_spending:
\code{\link{default_rules}},
\code{\link{example_data}},
\code{\link{std_categories}}
}
\concept{tracking_spending}
\concept{utils}
